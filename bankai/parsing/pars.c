#include "../include/prototype.h"


void    pars(t_glob *shell_kai)
{
    int i;

    i = 0;
    if (shell_kai->input)
    {
        while (shell_kai->input[i] == ' ')
            i++;
        
    }
}